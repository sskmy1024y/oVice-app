{
  "name": "ovice-app",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "run-p build:*",
    "build:mac": "electron-packager . oVice-app --platform=darwin --arch=arm64 --electronVersion=18.0.3 --out ./output/ --overwrite",
    "build:mac-x64": "electron-packager . oVice-app --platform=darwin --arch=x64 --electronVersion=18.0.3 --out ./output/ --overwrite",
    "build:win": "electron-packager . oVice-app --platform=win32 --electronVersion=18.0.3 --out ./output/ --overwrite",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "oVice",
    "desktop"
  ],
  "author": "sho(@sskmy1024y)",
  "license": "MIT",
  "dependencies": {
    "electron": "^18.0.3"
  },
  "devDependencies": {
    "asar": "^3.1.0",
    "electron-packager": "^15.4.0",
    "electron-reload": "^2.0.0-alpha.1",
    "npm-run-all": "^4.1.5"
  }
}
